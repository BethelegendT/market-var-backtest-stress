## Data format

CSV with two columns:
- `date` (YYYY-MM-DD)
- `close` (price)

This repo ships with a synthetic series (`sample_prices.csv`) so it runs offline.
Replace it with any daily close series to reproduce the workflow.
